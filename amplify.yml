version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Use npm install instead of npm ci (no lockfile committed)
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '*/'
  cache:
    paths:
      - node_modules/*/
    
